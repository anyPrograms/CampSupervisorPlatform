<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="src.main.java.com.csp.dao.StudentDao">
	<!-- 目的：为dao接口方法提供sql语句配置 -->
	<!-- <select id="findStudentById" resultType="Student" parameterType="String">
		
		SELECT
			s_id,
			s_name,
			s_bunk,
			s_status
		FROM
			student
		WHERE
			s_id = #{s_id};
	</select>
	
	<select id="findStudents" resultType="Student">
		SELECT
			s_id,
			s_name,
			s_bunk,
			s_status
		FROM
			student
		ORDER BY
			s_id
		LIMIT #{offset}, #{limit};
	</select>
	
	<insert id="addStudent" parameterType="Student">
		INSERT INTO Student (s_id,s_name,s_bunk,s_status)
		VALUES (#{s_id},#{s_name},#{s_bunk},#{s_status});
	</insert>
	
	<update id="updateStudent" parameterType="Student">
		UPDATE Student
		SET Student.s_name=#{s_name},
			Student.s_bunk=#{s_bunk},
			Student.s_status=#{s_status}
		WHERE Student.s_id=#{s_id};
	</update>
	
	<delete	id="deleteStudent" parameterType="String">
		DELETE FROM Student WHERE s_id = #{s_id};
	</delete>  -->
</mapper>